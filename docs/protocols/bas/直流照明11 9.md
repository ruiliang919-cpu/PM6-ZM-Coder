议题1：分区控制：开关控制按钮只是控制，还是有状态读取显示，若没有，应做两个按钮，分别是“开”、“关”

结论：需要两个按钮：“开”“关”，并且操作之后有反馈，可以通过背景颜色区分；

**接口： POST `/zm/baseDistrit/list`**

 

议题2：传感器信息页面显示为空，应显示传感器相对应参数

结论：待定

 

~~议题3：~~交流信息需要按照-遥测地址0x0028-0x002A 交流输入单双路、交流电流是否显示、交流采样模式单三相上传的值，显示相对应的内容（即可见度），现在同一只有按双路、电流三相显示，（0x0028取值反了，0x0029与0x002A功能地址取反了，取值也需要确认）；

结论：按照协议重新配置可见度

**接口： GET /zm/cabinetInfo/getAlternating** 还有一个交流 2 路

 

~~议题4：~~主机不显示PM6US交流二路输入状态，并且不应告警

结论：0x0700-0x073F开关状态，0x0136-0x0137状态不需要告警，并且0x0136-0x0137状态，需要在交流信息页面增加一路、二路工作状态；

**接口：GET `/zm/cabinetInfo/getAlternating?slaveId` 添加结果字段 workStatus(‘备用’:’工作’)**

 

~~议题5：~~需增加一个当前照度值，不启用外控时显示本机柜的值，启用外控时，通过外控地址以及外控通道显示，并且照度值、外控通道、外控地址需要实时读取，0XAE8F、0XAE96、0XAE9D、0XAEA4、0XAEAB

结论：协议中遥测增加5个照度值，后续给出寄存器地址，并且照度值、外控通道、外控地址需要实时读取

**接口：GET `/zm/sensorModule/getIllBaseLux?deviceId=&moduleId=` 根据照度传感器模式获取当前照度值，不启用外控下的照度值接口，需要一直读取**

 

议题6：对时保存按钮改成“启动对时”，提示信息也相对应更改；

结论：时保存按钮改成“启动对时”，提示信息：确定启动对时吗？前端更改

 

议题7：机柜控制-ACDC、DCDC模块电压输出控制，更改设置值后，应直接显示更改后的值，无须刷新页面显示，地址0xA007-0xA008；

结论：前端更改

 

议题8：建议项：设置参数时，有“确定”按钮，建议是可按回车执行该效果；

结论：前端更改，确认是否可以更改，

 

~~议题9：~~①普通时控与场景时控的“启用”按钮，应分01/02/03页面单独设置，现在某个页面启用或取消后，3个模式会同时操作，应有互斥，只能启用一个模式时段；②普通时控和场景时控，在模式01选择启用后保存下发，在场景2 不勾选启用保存下发，会把模式01的启用一并改成不选中，参数应不能影响；③与第1和第2个问题类似，点击一键设置按钮，也会把其他的时控模式是否启用不勾选；

结论：重新核对，优化，如果协议有歧义，请提出问题点，可优化协议；

 

议题10：**①“传感模式”需改成“传感器模式”**；②照度传感器启用参数未下发保存标志位，主监控未能掉电保存；③红外传感器启用参数未下发保存标志位，主监控未能掉电保存；

结论：核对主监控设备是否有把传感器启用按钮做到保存标志位里面

 

议题11：缺少策略显示界面：主机无法判断现在的控制方式处于什么状态下：普通时控、场景时控、红外传感、照度传感

时控选择、手动控制模式也要做出提示；（与问题44第2小点）

结论：前端更改，增加普通时控、场景时控、手动控制（回路控制、分组控制、场景控制）按钮提示

**接口：GET  `/zm/write/getModuleNow`** 可能需要添加两列 手动模式选择 和 时控模式 的展示

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    // 手动模式选择 loop 回路控制/ group 分组控制 / scene 场景控制
    "handModule": "loop",
    // 照度传感模式：false 为未选中，true 为选中
    "illuminanceSensorModule": false,
    // 红外传感模式：false 为未选中，true 为选中
    "infraredSensorModule": false,
    // 时控模式 noEnabled 未开启时控模式 / simple 普通时控模式 / scene 场景时控模式
    "timeModule": "simple"
  }
}
```

 

议题12：回路控制和分组控制，开关按钮名称有误：应是开、关；

结论：前端更改：启动改成：开；停止改成：关

 

议题13：缺少交流开关控制页面；

结论：待定



~~议题14~~：主机本地部署后，电脑关机之后，重新开机时，无法登陆平台程序：网页显示系统接口502异常；

结论：已经解决，但是未部署到我司主机，后续部署验证。

 

议题15：当前告警页面，“机器名称”，改成“箱柜名称”；

结论：前端更改，

 

议题16：总开关状态、工作模式、控制方式，需要作实时刷新；

结论：前端更改

 

议题17：基本资料-设备区域、分区设置、场景设置、机柜设置中的“系统编号”，需要改成“序号”；

结论：前端更改

 

议题18：①主机连续挂两天后，与上位机通讯，返回数据时有时无，导致上位机报通讯故障（2024-10-24,23点开始，一直到25号早上九点，重启主机后恢复正常）；②主机运行过程中会出现与上位机通讯异常（通讯质量差，不返回），重启服务器之后，通讯恢复正常，并且查询间隔时间需要2000ms以上；

结论：对接上位机功能，单独拎出来，后续验证，查询间隔时间需要500ms

 

~~议题19~~：机柜A12分组添加回路后，不显示具体回路路数，现在A12可以显示回路，但是A13又不能显示，（A12 IP 比A13 IP小）（必须是读取过一次设置更新总点才能保存）；

结论：已经找到问题原因，已经优化，待部署完成后验证；

 

议题20：分区组合设置，只能挨个组合设置保存，“保存所有”按钮不起作用，例如在分区组合01页面选中123分区后，切换到分组组合02选中456保存，只能保存分区02选中的456；

结论：前端更改，把“保存所有”改成“保存”，每个分区组合单独设置。

 

~~议题21~~ 主机与上位机通讯，未上传场景模式；

结论：改成正常通讯最小IP，手动模式下的场景上传，自动按自动状态下场景上传；我这边把BAS自动变手动，再恢复自动的控制逻辑重新整理并发出来；

 

议题22：就地/远程按钮增加到所有界面都可以操作，且切换了就地远程后，主机本地需要能识别出来

结论：前端更改

**接口： GET `/zm/point/getModule`**

```json
{
   "code":200,
   "msg":"操作成功",
   // 当前的模式 0-就地 1-远程
   "data": 0
 }
```

 

议题23：增加主机版本号；

结论：需要增加，需要优化

**接口： GET `/zm/home/version`**

```json
{
  "code":200,
  "msg":"操作成功",
  // 照明系统版本号
  "data":"V0.0.1"
}
```

 

议题24：①首页固件版本改成主监控版本；②右上角快捷栏去掉“升级”、“通讯”按钮；③右上角“导入遥信协议”改成“导入故障定义文本”；

结论：前端更改

 

议题25：1、机柜综合电量统计页面，如果只是做显示，对上位机传电量没有影响，可删除

2、如果不删除，那只显示电源柜的电量，而且只显示7行即可，时间与电量在读取时刷新；

结论：功能保留，界面隐藏

**删除机柜综合电量统计的页面**

 

议题26：全局场景控制页面，控制场景后，应能直接**刷新**到该场景；

结论：前端更改

 

~~议题27~~：A12-与A13通讯正常情况下，A12可以更改分区名称和分区码，且能同时下发，A13机柜只能更改分组01的名称，02-16名称更改可以下发到设备，但是本地没有显示更改；

结论：已经找到问题原因，已经优化，待部署完成后验证；

 

~~议题28~~：主机索引开关状态上传时，未按照24-31分区码排序，（现在是如果是25排在24前面，就先索引分区码25的回路，应是先索引分区码24的回路）

结论：分区码24-31固定只有一个分区，索引规则按照24-31分区码排序，不管分组先后；

 

议题29：电量计算：

1、 分区组合计算，选择分区组合后应直接计算出当前日、周、月、季、年的耗电量，（

计算逻辑：某个分组组合电量进行分区组合配置后，统计此组合下所有机柜回路，每个机柜先筛选出重复回路进行剔除，确保每个机柜的相同回路只计算一次，然后累加，最后再把所有机柜的电量累加；

2、配置了分区组合后，统计电量有误，未能把所有机柜的回路累加，按以上逻辑

结论：1，分组组合配置完成后，点击保存按钮，就会计算出电量，2、重新核对电量计算，按以上逻辑

未发现错误，应该是设备回路分组的缓存未处理好（没有设置更新总点，导致回路分组的数据不是最新的数据），导致查询设备的数据出错

 

议题30：重启主机或重启服务器后，开关状态就不上传后台了，一直显示关的状态；

结论：重新核对代码

未发现错误，可能监控修改了回路分组数据，但是没有设置更新总点将数据上传到主机

 

~~议题31~~：设备与主机通讯断后，设备恢复通讯时，主机应该全部下发一遍遥调、遥控值

结论：功能需要添加（开发部门内部讨论功能如何实现）；

**迁移设备数据 GET /recover/issued**

```json
{
  "code": 200,
  "msg": "迁移设备数据指令已下发",
  "data": null
}
```

**读取设备最新数据 GET /recover/read** 

```json
{
  "code": 200,
  "msg": "读取设备最新数据指令已下发",
  "data": null
}
```

